services:
  - type: web
    name: grandgaz-f1-proxy
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000 --worker-class sync --workers 2
    plan: free
